/**
 * 登录组件
 */
import Login, { LoginProps } from './Login'
import Layout, {
  LayoutProps,
  Alert,
  Button,
  Input,
  defaultBackground,
  defaultLogo,
} from '../login-layout'

export default Login
export {
  LoginProps,
  Layout,
  Alert,
  LayoutProps,
  Button,
  Input,
  defaultBackground,
  defaultLogo,
}
